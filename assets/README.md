# assets

Put the hero avatar photo here as `lily.jpg` (referenced by `landlord.html`).

Recommended: a square-ish headshot, at least 300×300px, JPG or PNG.
If you use a different filename or format, update the `<img src="assets/lily.jpg">`
line in `landlord.html` to match.
